# =============================================================================
# Opcional: segundo serviço só com ficheiros estáticos (SPA noutro domínio).
# No iOS/Android os cookies entre dois *.onrender.com falham com frequência.
#
# Recomendação: um único Web Service com build em deploy/render-build.sh
# e CORS + cookies só para a mesma URL da API.
# =============================================================================

# Se mantiveres este serviço separado, aponta para a API (sem barra no fim).
VITE_API_URL=https://futball-manager.onrender.com
