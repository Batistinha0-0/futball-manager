# =============================================================================
# Frontend (SPA estático) — Render — copie para "Environment" do serviço do front
# URL do SPA: https://futball-manager-1.onrender.com
# URL da API (serviço separado): https://futball-manager.onrender.com
# =============================================================================

# Sem barra no fim. O build do Vite embute este valor em runtime (import.meta.env).
VITE_API_URL=https://futball-manager.onrender.com
